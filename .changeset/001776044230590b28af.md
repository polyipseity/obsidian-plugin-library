---
"@polyipseity/obsidian-plugin-library": patch
---

Replace lodash-es and deep-equal with es-toolkit

- Add es-toolkit as dependency and remove lodash-es and deep-equal
- Migrate core modules, build script, and remaining source modules to es-toolkit
- Remove the temporary internal helpers module after migration

Harden linting with eslint-plugin-obsidianmd and strict type checking

- Integrate eslint-plugin-obsidianmd and switch ESLint to strictTypeChecked with projectService
- Add descriptions to eslint-disable directives across source, tests, and scripts
- Resolve lint and type errors across source, tests, and scripts, including replacing innerHTML with DOMParser, inline style assignments with CSS classes, and globalThis with window

Harden tests and scripts

- Convert test files from .mjs to .ts with strict-mode fixes
- Convert scripts and tests from sync to async fs/promises and async exec
- Adopt Promise.allSettled + AggregateError cleanup patterns in build script
- Parse JSON and YAML with valibot schemas instead of raw JSON.parse and parseYaml
- Add type safety to test files

Improve types

- Make $CommunityPluginsSettingTab installedPlugins and renderInstalledPlugin optional
- Rename @types/dom.d.ts to @types/lib.dom.ts and re-export it from index
- Move valibot from devDependencies to dependencies

Throw on invalid or empty SVG content in icons

- Convert README inline links to reference style
